server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MaximilianoTRZ/tasks-manager-ms-config
          clone-on-start: true
